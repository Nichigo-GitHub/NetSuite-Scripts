"use strict";

/**
 * @NApiVersion 2.x
 * @NScriptType ClientScript
 */
define(['N/search', 'N/currentRecord'], function (search, currentRecord) {
  function pageInit(context) {// Get the current record object from the context
  }

  return {
    pageInit: pageInit
  };
});